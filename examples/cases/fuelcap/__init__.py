"""Case 27 — the inner fuel cap (内盖), as an infinite labelled dataset.

`parts` models the pocket, `scene` randomises and assembles one, `dataset` renders it into
the same npz a real capture becomes, and `fit` is the measurement the whole thing is built
from. `sheet` looks at any of it alone.
"""
